# Runtime dependencies for the MLX path. PyTorch is deliberately omitted: the
# setup script creates a venv with access to the already-installed PyTorch.
mlx==0.30.6
nanobind==2.11.0
numpy==2.4.2
pillow==12.1.1
plyfile==1.1.3
setuptools==80.10.2
tqdm==4.67.3
viser==1.0.21

